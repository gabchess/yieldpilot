import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "YieldPilot - AI Copilot for Cross-Chain Yield Optimization",
  description:
    "Maximize your DeFi yields with AI-powered cross-chain portfolio optimization. Real-time analysis of lending, staking, and LP opportunities across Ethereum, Arbitrum, Base, and more.",
  keywords: [
    "DeFi",
    "yield optimization",
    "AI",
    "cross-chain",
    "portfolio management",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
      </body>
    </html>
  );
}
